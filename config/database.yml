
development: &development
 adapter: mysql2
 strict: false
 database: expense_management
 username: root
 host: localhost
 password: vikivinith
 #socket: /private/tmp/mysql.sock

# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.

# Testing Database Note: In order to use parallel_tests, you need to switch database to "database: coupa_test<%= ENV['TEST_ENV_NUMBER'] %>"
# test: &test
#  adapter: mysql2
#  strict: false
#  database: 
#  username: root
#  host: localhost
#  password: vikivinith

production:
  <<: *development
